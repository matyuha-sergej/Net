# Быстрый старт

## 1. Установка зависимостей

```bash
# Активируйте виртуальное окружение
source .venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

## 2. Проверка работоспособности

```bash
# Запустите тестовый скрипт
python test_app.py
```

Это создаст тестовое изображение и проверит все модули.

## 3. Запуск приложения

### Вариант А: Графический интерфейс (рекомендуется для начала)

```bash
python main.py
```

**Действия в GUI:**
1. Нажмите "Загрузить изображение"
2. Выберите изображение с мячом и сеткой
3. Убедитесь, что выбрана модель (по умолчанию yolov8n.pt)
4. Выберите метод детекции сетки (попробуйте hough)
5. Нажмите "Анализировать"

### Вариант Б: Командная строка

```bash
# Создайте тестовое изображение
python test_app.py

# Проанализируйте его
python cli.py test_image.jpg
```

## 4. Работа с вашими датасетами YOLO

### Если у вас уже есть обученная модель (.pt файл):

```bash
# GUI
python main.py
# Выберите "Своя модель..." и укажите путь к .pt файлу

# CLI
python cli.py your_image.jpg --model path/to/your_model.pt
```

### Если нужно обучить модель на ваших данных:

```bash
# 1. Создайте пример конфигурации
python train_yolo.py --create-example

# 2. Отредактируйте data.yaml, указав путь к вашему датасету

# 3. Запустите обучение
python train_yolo.py --data data.yaml --epochs 100 --batch 16

# 4. Используйте обученную модель
python cli.py image.jpg --model runs/detect/ball_detector/weights/best.pt
```

## 5. Обработка видео

```bash
python cli.py video.mp4 --video --model your_model.pt --output result.mp4
```

## Структура датасета YOLO

Ваш датасет должен иметь такую структуру:

```
dataset/
├── images/
│   ├── train/
│   │   ├── img001.jpg
│   │   ├── img002.jpg
│   │   └── ...
│   ├── val/
│   │   └── ...
│   └── test/
│       └── ...
└── labels/
    ├── train/
    │   ├── img001.txt
    │   ├── img002.txt
    │   └── ...
    ├── val/
    │   └── ...
    └── test/
        └── ...
```

Формат файлов разметки (.txt):
```
class x_center y_center width height
```

Например:
```
0 0.5 0.6 0.1 0.1
```

Где все значения нормализованы от 0 до 1.

## Методы детекции сетки

Если один метод не работает, попробуйте другой:

- **hough** - лучше для четких контрастных сеток
- **contour** - лучше для сеток с явными границами
- **color** - лучше для белых сеток на темном фоне

## Решение проблем

### Модель не загружается
```bash
# Убедитесь, что установлен PyTorch
pip install torch torchvision

# Проверьте версию
python -c "import torch; print(torch.__version__)"
```

### Сетка не обнаруживается
- Попробуйте разные методы (hough, contour, color)
- Убедитесь, что сетка хорошо видна на изображении
- Можно настроить параметры в `net_detector.py`

### Низкая точность детекции
- Обучите модель на своих данных
- Увеличьте порог уверенности
- Используйте более мощную модель (yolov8m.pt или yolov8l.pt)

## Примеры команд

```bash
# Базовый анализ
python cli.py image.jpg

# С настройками
python cli.py image.jpg --model yolov8s.pt --confidence 0.7 --net-method color

# Видео
python cli.py match.mp4 --video --confidence 0.6

# GUI
python main.py
```

## Следующие шаги

1. Проверьте работу на тестовом изображении
2. Попробуйте на своих изображениях
3. Если точность низкая - обучите модель на своих данных
4. Экспериментируйте с методами детекции сетки
5. Для больших объемов используйте CLI для пакетной обработки

Удачи! 🎾⚽🏐
