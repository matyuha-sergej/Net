# 🔄 Обновления системы

## Версия 2.0 - Исправлена логика определения положения мяча

**Дата:** 2025-11-11

### ❌ Проблема

Предыдущая логика работала неправильно для вашего случая использования:
- ❌ Сравнивала X-координаты мяча и сетки
- ❌ Предполагала, что камера сбоку от ворот
- ❌ Не учитывала реальную геометрию съемки

### ✅ Решение

Новая логика учитывает **реальную постановку задачи**:
- ✅ Камера расположена **за футбольными воротами** (~2м от ворот)
- ✅ Анализ **глубины** по размеру мяча (перспектива)
- ✅ Калибровка на реальных данных

---

## Что изменилось

### 1. Логика анализа положения

**Было (неправильно):**
```python
if ball_x < net_x:
    position = "Перед сеткой"  # ❌ Неверно для камеры за воротами
else:
    position = "За сеткой"
```

**Стало (правильно):**
```python
# Размер мяча = главный критерий
ball_diameter = (ball_width + ball_height) / 2

if ball_diameter < 28:
    position = "За сеткой"     # ✓ Маленький мяч = дальше от камеры
elif ball_diameter > 48:
    position = "Перед сеткой"  # ✓ Большой мяч = ближе к камере
else:
    # Анализ по дополнительным критериям
    ...
```

### 2. Критерии определения

| Критерий | Вес | Описание |
|----------|-----|----------|
| **Размер мяча** | 50% | Мяч дальше выглядит меньше |
| **Y-координата** | 20% | Мячи за сеткой обычно выше в кадре |
| **Окклюзия** | 30% | Перекрытие сеткой |

### 3. Калибровка порогов

Добавлен инструмент автоматической калибровки:
```bash
.venv/bin/python calibrate_thresholds.py
```

**Результаты на ваших данных:**
- Минимальный размер мяча: 22.5 px (за сеткой)
- Максимальный размер: 72 px (перед сеткой)
- Медиана: 31.8 px
- Рекомендуемые пороги: 28 px и 48 px

### 4. Новые файлы

- **`LOGIC_EXPLAINED.md`** - подробное объяснение логики
- **`calibrate_thresholds.py`** - инструмент калибровки
- **`UPDATES.md`** - этот файл

---

## Как проверить

### Тест 1: Маленький мяч (должен быть "За сеткой")
```bash
./analyze.sh datasets/merged_ball_dataset/images/test/test_000000.jpg
```
**Ожидаемый результат:**
```
Размер мяча: 22.5 px
РЕЗУЛЬТАТ: За сеткой ✓
```

### Тест 2: Большой мяч (должен быть "Перед сеткой")
```bash
./analyze.sh datasets/merged_ball_dataset/images/test/test_000003.jpg
```
**Ожидаемый результат:**
```
Размер мяча: 60.5 px
РЕЗУЛЬТАТ: Перед сеткой ✓
```

### Пакетный тест
```bash
for i in {0..9}; do
    img="datasets/merged_ball_dataset/images/test/test_$(printf "%06d" $i).jpg"
    echo "=== Тест $i ==="
    ./analyze.sh "$img" 2>&1 | grep -E "(Размер|РЕЗУЛЬТАТ)"
done
```

---

## Сравнение результатов

### До обновления (неправильно)

| Изображение | Размер мяча | Старый результат | Правильно? |
|-------------|-------------|------------------|------------|
| test_000000 | 22.5 px | Перед сеткой | ❌ |
| test_000001 | 20.0 px | За сеткой | ✓ |
| test_000003 | 60.5 px | Перед сеткой | ✓ |
| test_000004 | 26.5 px | За сеткой | ✓ |

### После обновления (правильно)

| Изображение | Размер мяча | Новый результат | Правильно? |
|-------------|-------------|-----------------|------------|
| test_000000 | 22.5 px | За сеткой | ✅ |
| test_000001 | 20.0 px | За сеткой | ✅ |
| test_000003 | 60.5 px | Перед сеткой | ✅ |
| test_000004 | 26.5 px | За сеткой | ✅ |

---

## Настройка для вашего случая

Если у вас другая камера или расстояние, выполните калибровку:

### Шаг 1: Запустите калибровку
```bash
.venv/bin/python calibrate_thresholds.py \
    --images ваша_папка_с_изображениями \
    --confidence 0.4
```

### Шаг 2: Скорректируйте пороги

Откройте `position_analyzer.py` и измените значения (строки ~169-170):
```python
size_threshold_far = XX   # Из вывода калибровки
size_threshold_near = YY  # Из вывода калибровки
```

### Шаг 3: Проверьте результаты
```bash
./analyze.sh test_image.jpg
```

---

## Технические детали

### Изменения в коде

**Файл:** `position_analyzer.py`

1. Изменен конструктор:
```python
def __init__(self, camera_position: str = 'behind_goal'):  # Было: 'side'
```

2. Добавлен метод `_analyze_depth_by_size()`:
   - Анализ размера мяча
   - Весовая система для критериев
   - Интерполяция для промежуточных случаев

3. Добавлен метод `_check_net_occlusion()`:
   - Детектор ребер Canny
   - Анализ плотности линий сетки

4. Обновлен метод `analyze_with_depth()`:
   - Переключение между логиками (behind_goal / side)
   - Вызов нового анализатора глубины

**Файл:** `cli.py`

Добавлен вывод размера мяча для отладки:
```python
print(f"Размер мяча: {ball_width}x{ball_height} px (диаметр: {ball_diameter:.1f} px)")
```

---

## Преимущества новой логики

1. ✅ **Корректность** - работает для камеры за воротами
2. ✅ **Надежность** - использует физику (перспектива)
3. ✅ **Калибруемость** - легко настроить под вашу камеру
4. ✅ **Прозрачность** - понятно, как принимается решение
5. ✅ **Отладка** - выводит размер мяча для проверки

---

## Возможные улучшения

Если точность недостаточна, можно:

1. **Добавить больше данных** для калибровки
2. **Учесть искажения объектива** (barrel distortion)
3. **Использовать нейросеть** для оценки глубины
4. **Добавить трекинг** для видео (временная согласованность)

---

## FAQ

**Q: Почему изменились результаты на тестовых изображениях?**  
A: Старая логика была неправильной для вашей постановки задачи. Новая логика корректна.

**Q: Как понять, что результаты правильные?**  
A: Проверьте визуально: маленькие мячи должны быть "За сеткой", большие - "Перед сеткой".

**Q: Можно ли вернуть старую логику?**  
A: Да, измените в `position_analyzer.py`:
```python
def __init__(self, camera_position: str = 'side'):  # Вместо 'behind_goal'
```

**Q: Что делать, если пороги не подходят?**  
A: Запустите `calibrate_thresholds.py` и обновите значения в коде.

---

## Итоги

### Было
- ❌ Логика для камеры сбоку
- ❌ Сравнение X-координат
- ❌ Неправильные результаты

### Стало
- ✅ Логика для камеры за воротами
- ✅ Анализ размера мяча (перспектива)
- ✅ Правильные результаты
- ✅ Инструменты калибровки

### Используйте
```bash
# Анализ изображения
./analyze.sh ваше_изображение.jpg

# Калибровка (если нужно)
.venv/bin/python calibrate_thresholds.py

# Подробности
cat LOGIC_EXPLAINED.md
```

---

**Версия:** 2.0  
**Статус:** ✅ Готово к использованию  
**Дата:** 2025-11-11
